import CheckoutMainArea from "@components/checkout/checkout-main";

export const metadata = {
  title: "Checkout - Serravit",
};

const Checkout = () => {
  return (
    <CheckoutMainArea/>
  );
};

export default Checkout;
